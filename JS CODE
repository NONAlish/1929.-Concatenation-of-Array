/**
 * @param {number[]} nums
 * @return {number[]}
 */
var getConcatenation = function(nums) {
    let newNums = []
    for (let i = 0; i < 2; i++) {

        for (let num of nums) {
            newNums.push(num);
        }   
    }
    
    return newNums;
};
